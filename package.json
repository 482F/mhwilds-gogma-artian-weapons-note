{
  "name": "mhwilds-gogma-artian-weapons-note",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "deno task build && git wa gh-page || true; dir=.git/worktree-dir/gh-page; rsync -a dist/ $dir && cd $dir && git a . && git zco -m deploy || true; git pushsu"
  },
  "dependencies": {
    "es-toolkit": "^1.43.0",
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/tsconfig": "^0.8.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vue-tsc": "^3.1.4"
  }
}
